# Python dependencies for libclang C Parser API

# Web framework
Flask==3.0.0
Flask-CORS==4.0.0

# libclang Python bindings  
libclang==16.0.6

# HTTP server (production ready)
gunicorn==21.2.0

# Development and testing (optional)
requests==2.31.0
